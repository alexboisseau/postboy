import RequestResponse from "@/components/RequestResponse/RequestResponse";

export default function Home() {
  return (
    <main className="py-5 max-w-screen min-h-screen flex flex-col items-center justify-center">
      <RequestResponse />
    </main>
  );
}
